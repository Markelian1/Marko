services:
  - type: web
    name: regjistrime-faturash
    runtime: static
    buildCommand: "echo 'No build step needed'"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
